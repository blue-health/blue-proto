syntax = "proto3";

package identity;

option go_package = "/identity";

service Service {
  rpc Get(GetRequest) returns (GetReply) {}
}

message GetRequest { string id = 1; }

message GetReply { string id = 1; }
