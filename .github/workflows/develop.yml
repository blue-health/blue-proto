name: Develop

on:
  push:
    branches:
      - develop
    paths-ignore:
      - ".local/**"
      - "reference/**"
      - "generated/**"

jobs:
  cd:
    name: CD
    uses: blue-health/blue-proto/.github/workflows/cd.yml@develop
